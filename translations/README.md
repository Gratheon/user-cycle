# Translations Snapshot

`user-cycle-translations.json` is generated from local DB with:

```bash
just export-translations
```

`restart.sh` imports this file automatically in production if it exists.
